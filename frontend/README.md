# Skill2Startup AI Frontend

React, Vite, Tailwind v4, and Framer Motion frontend for the Skill2Startup AI FastAPI backend.

## Commands

```bash
npm install
npm run dev
npm run build
```

Leave `VITE_API_URL` empty in local development to use the Vite proxy for `http://127.0.0.1:8000`.
